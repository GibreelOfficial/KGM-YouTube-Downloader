#windowContainer {
    background-color: {{primary_bg}};
    border: 2px solid {{border_color}};
    border-radius: 10px;
}

#minBtn, #maxBtn, #closeBtn {
    background: transparent;
    color: {{text_main}};
    border: none;
    font-size: 16px;
    min-width: 18px;
    max-width: 18px;
    min-height: 18px;
    max-height: 18px;
    border-radius: 6px; 
}

#CustomTitleBar {
    border-bottom: 2px solid {{border_color}};
    color: {{text_main}};
    font-weight: normal;
    font-size: 13px;
}

#closeBtn:hover {
    background-color: {{danger}};
}
#minBtn:hover {
    background-color: {{ready}};
}
#maxBtn:hover {
    background-color: {{gogo}};
}

#sidebar {
    background-color: {{secondary_bg}};
    border-right: 2px solid {{border_color}};
}

/* New Input Fields Matching App Layout Mechanics with Dynamic Theme Background */
QLineEdit#urlInput {
    background-color: {{secondary_bg}};
    border: 1px solid #444;
    border-radius: 5px;
    color: {{text_main}};
    padding-left: 12px;
    font-size: 13px;
}

/* Control Buttons & Pop-up Toggles Matching Stroke Profiles */
QPushButton#fetchUrlBtn, QPushButton#queueWindowBtn {
    background-color: transparent;
    border: 2px solid {{border_color}};
    border-radius: 6px;
    color: {{text_main}};
    font-weight: bold;
}

QPushButton#fetchUrlBtn:hover, QPushButton#queueWindowBtn:hover {
    background-color: rgba(255, 255, 255, 0.05);
}

QPushButton#browseBtn {
    background-color: transparent;
    border: 2px solid {{border_color}};
    border-radius: 6px;
    color: {{text_main}};
    padding-left: 12px;
    padding-right: 12px;
    font-size: 12px;
}

QPushButton#browseBtn:hover {
    background-color: rgba(255, 255, 255, 0.05);
}

#destLabel {
    color: {{text_main}};
    font-weight: bold;
}

#detailPathDisplay {
    color: {{text_main}};
}

/* Custom progress bar using the accent color */
QProgressBar {
    border: 1px solid #444;
    border-radius: 5px;
    background: #222;
    text-align: right;
    margin-right: 40px;
}

QProgressBar::chunk {
    border-radius: 4px;
}

.progress-complete::chunk { background-color: #8bc34a; }
.progress-paused::chunk   { background-color: #ff9800; }
.progress-failed::chunk   { background-color: #f44336; }
.progress-queued::chunk   { background-color: #03a9f4; }

/* Dashed Drop Zone */
#addUrlBtn {
    background-color: transparent;
    border: 2px dashed #444444;
    border-radius: 15px;
    color: {{text_main}};
    font-size: 20px;
}

#addUrlBtn:hover {
    border-color: #00ffcc;
    background-color: rgba(0, 255, 204, 0.05);
}

/* Info Panel at the bottom */
#infoPanel {
    background-color: rgba(255, 255, 255, 0.02);
    border-top: 2px solid #333333;
    border-radius: 12px;
}

#statusBar {
    border-top: 2px solid {{border_color}};
    color: {{text_main}};
}

#statusBar QLabel {
    color: {{text_main}};
    font-size: 12px;
}

/* The Table Container */
#customTable {
    background-color: {{primary_bg}};
    alternate-background-color: {{primary_bg}};
    selection-background-color: {{secondary_bg}};
    selection-color: {{text_main}};
    outline: none;
    border: none;
    color: {{text_main}};
}

/* Row Styling */
#customTable::item {
    padding: 3px;
}

#customTable::item:selected {
    background-color: {{secondary_bg}};
    border: 2px solid {{ready}};
}

/* Header Styling - CamelCase & Tight Height */
QHeaderView::section {
    background-color: {{primary_bg}};
    color: {{text_main}};
    padding-left: 15px;
    color: {{text_main}};
    border: 1px solid {{border_color}};
    font-weight: 300;
    font-size: 12px;
}

/* Scrollbar remains minimalist */
QScrollBar:vertical {
    border: none;
    background: transparent;
    width: 6px;
    margin: 0px;
}

QScrollBar::handle:vertical {
    background: {{border_color}};
    min-height: 20px;
    border-radius: 3px;
}

QPushButton#statusBarUpdateBtn {
    background-color: transparent;
    border: none;
    color: {{text_main}};
    font-size: 12px;
    padding-left: 5px;
    padding-right: 5px;
}

QPushButton#statusBarUpdateBtn:hover {
    color: #03a9f4;
}